# ============================================================
#  groups_extra.ps1 ─ 你自己的補充分類（選用）
#
#  groups.ps1 最後會自動載入這個檔；同一個代碼以這裡為準。
#  好處：之後若拿到新版 groups.ps1，直接覆蓋就好，你的補充不會被蓋掉。
#
#  怎麼加：把下面範例那行開頭的 # 拿掉，改成你的代碼與族群名，一行一檔。
#  族群名盡量沿用 groups.ps1 既有名稱（例如「被動元件」「AI伺服器」），報告才會一致；
#  要開新族群也可以，直接寫新名字。
#  存檔格式：UTF-8（含 BOM）。
# ============================================================

$extra = @{
    # '2330'='晶圓代工'      # 台積電（範例：已在 groups.ps1，這裡寫了會覆寫）
    # '9999'='新族群名'      # 範例：開一個新族群
}

foreach ($k in $extra.Keys) { $Global:StockGroups[$k] = $extra[$k] }
