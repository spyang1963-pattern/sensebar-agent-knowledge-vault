# 2026-08-02 工作紀錄

## 今日完成

### 1. 金融交易 136 部影片 KB 收成位置錯誤修正
**問題根因**：
- 三個課程（謝佳穎 60 / 唐在信 41 / 林國寶 35）任務的 `kb_subpath` 以 `/` 開頭（`/金融交易/謝佳穎/`）
- Windows `os.path.join()` / pathlib 遇以 `/` 開頭的絕對路徑會**丟棄**前面的 `knowledge-base`
- 結果全部誤寫到 D 槽根目錄 `D:\金融交易\`，Obsidian 看不到

**修正項目**：
| 檔案 | 改動 |
|------|------|
| `scheduler.py` harvest() | `kb_subpath = kb_subpath.strip().strip('/\\')` |
| `file_processors.py` | 新增 `_kb_dir()` helper，統一去除前後斜線 |
| `task_manager_gui.py` | 寫入任務前 `.strip('/\\')`（video/audio/document/web 皆套用） |

**檔案搬移**：136 個 md 已從 `D:\金融交易\` 移回 `knowledge-base\金融交易\`，並按來源子目錄分層。

### 2. 金融交易目錄結構分層
**問題**：新增三個課程全部拍平在課程根層。
**修正**：依來源子目錄重排（比照既有課程慣例）：
- `謝佳穎/`：理周(47)、今學堂(8)、主力控盤操作學(3)、主控戰略K線(2)
- `唐在信/`：589(33)、演講(4)、解開江波圖(4)
- `林國寶/`：高手-楊榮熾(23)、技術分析(8)、波浪量化3.0(4)

### 3. 音訊轉逐字稿功能強化
**問題**：GUI 把音訊**目錄**當成單一檔案送給轉錄腳本 → PermissionError。
**修正項目**：
| 檔案 | 改動 |
|------|------|
| `file_processors.py` | `process_audio` 支援目錄：遞迴掃描音訊檔、依來源子目錄分層收成、回報進度（index/total）、支援 `partial` 結果、**續傳跳過已收成檔** |
| `task_manager_gui.py` | audio 表單新增「資料夾」按鈕 |

### 4. 大檔音訊切段轉錄
**問題**：70 個音訊檔 >24MB，壓縮後仍超 Groq 上傳上限，全部失敗。
**修正**：`transcribe_groq.py` 新增切段轉錄：
- `transcribe_in_chunks()`：ffmpeg 切段（預設每段 15 分鐘）逐段轉錄
- `merge_results()`：合併分段結果，時序碼平移 offset 秒
- `--chunk-min` 參數可調段長
- 驗證：117 分鐘音訊切 8 段轉錄成功，時間碼正確（末段 01:56:45）

## 待處理
- 任務 #1064：69 個大檔續傳中（PID 9632），完成後檢核 KB 完整性
- 御神書第2冊 OCR：等 PC1/PC2 設定好再作業

## 系統狀態
- GUI (PID 9324) 運行中（載入舊 code，功能正常；新 code 需重啟生效）
- 任務 #1064 audio 續傳中
- Groq 額度：音訊轉錄走 ASD（音訊秒數/日），與 OCR TPD 分開計算

## 備份位置
- 系統備份：`D:\AI-Agent-Workspace\backup_20260802_*\`
