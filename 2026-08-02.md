# 2026-08-02 工作紀錄

## 今日完成

### 1. 金融交易 136 部影片 KB 收成位置錯誤修正
**問題根因**：
- 三個課程（謝佳穎 60 / 唐在信 41 / 林國寶 35）任務的 `kb_subpath` 以 `/` 開頭（`/金融交易/謝佳穎/`）
- Windows `os.path.join()` / pathlib 遇以 `/` 開頭的絕對路徑會**丟棄**前面的 `knowledge-base`
- 結果全部誤寫到 D 槽根目錄 `D:\金融交易\`，Obsidian 看不到

**修正項目**：
| 檔案 | 改動 |
|------|------|
| `scheduler.py` harvest() | `kb_subpath = kb_subpath.strip().strip('/\\')` |
| `file_processors.py` | 新增 `_kb_dir()` helper，統一去除前後斜線 |
| `task_manager_gui.py` | 寫入任務前 `.strip('/\\')`（video/audio/document/web 皆套用） |

**檔案搬移**：136 個 md 已從 `D:\金融交易\` 移回 `knowledge-base\金融交易\`，並按來源子目錄分層。

### 2. 金融交易目錄結構分層
**問題**：新增三個課程全部拍平在課程根層。
**修正**：依來源子目錄重排（比照既有課程慣例）：
- `謝佳穎/`：理周(47)、今學堂(8)、主力控盤操作學(3)、主控戰略K線(2)
- `唐在信/`：589(33)、演講(4)、解開江波圖(4)
- `林國寶/`：高手-楊榮熾(23)、技術分析(8)、波浪量化3.0(4)

### 3. 音訊轉逐字稿功能強化
**問題**：GUI 把音訊**目錄**當成單一檔案送給轉錄腳本 → PermissionError。
**修正項目**：
| 檔案 | 改動 |
|------|------|
| `file_processors.py` | `process_audio` 支援目錄：遞迴掃描音訊檔、依來源子目錄分層收成、回報進度（index/total）、支援 `partial` 結果、**續傳跳過已收成檔** |
| `task_manager_gui.py` | audio 表單新增「資料夾」按鈕 |

### 4. 大檔音訊切段轉錄
**問題**：70 個音訊檔 >24MB，壓縮後仍超 Groq 上傳上限，全部失敗。
**修正**：`transcribe_groq.py` 新增切段轉錄：
- `transcribe_in_chunks()`：ffmpeg 切段（預設每段 15 分鐘）逐段轉錄
- `merge_results()`：合併分段結果，時序碼平移 offset 秒
- `--chunk-min` 參數可調段長
- 驗證：117 分鐘音訊切 8 段轉錄成功，時間碼正確（末段 01:56:45）

### 5. 呂佳霖影片 KB 收成目錄結構修正
**問題**：影片來源 `H:\!呂佳霖-箱波均系列課程&每日盤後分析(有字幕)` 有多層子目錄，但 `harvest()` 在 `kb_subpath` 存在時只用 `basename` 平鋪 → 611 個 md 全擠在 `金融交易\呂佳霖\` 頂層。
**修正項目**：
| 檔案 | 改動 |
|------|------|
| `scheduler.py` | 新增 `_video_kb_subdir()` helper，依 `video_relpath` 中「課程根目錄之後的子目錄結構」分層（與 audio 的 `relative_to` 一致） |

**檔案搬移**：611 個 md 依來源子目錄重排至 5 層結構（每日盤後分析 485 / 箱波均-進階課程 55 / 嗨投資購買課程 38 / VCD購買課程 30 / 盤後分析特別錄製 3），與來源 H: 槽結構一致。

### 6. PDF OCR 改用 Gemini（御神書第2冊完成）
**背景**：原 Groq Vision OCR 品質差（600px/Q30 逐頁 25 秒）且吃 TPD 額度。
**新增 `_ocr_pdf_gemini()`**（`file_processors.py`）：
- 原生 PDF inline bytes 上傳（免逐頁轉圖），每 10 頁一段
- 頁碼解析：切割後段內頁碼 + 段起始頁偏移 → 絕對頁碼
- 支援中斷續傳 + 併發防護 + 進度回報（沿用既有檢查點機制）
- RPM/每日額度正確分類：每日額度停止續傳，RPM 依建議秒數重試
- 503 UNAVAILABLE 自動等待重試
- 降級鏈：Gemini → Groq Vision → Tesseract

**御神書第2冊（193 頁）收成**：`knowledge-base\文件\新健康協會\御神書\御神書第2冊.md`（174,514 字，193 頁全，無缺頁無重複）。

**額度探索**（Gemini 免費 tier）：
- `gemini-3.5-flash` / `gemini-3.6-flash`：每天僅 20 次請求，無法跑整本 OCR
- `gemini-2.5-flash` / `gemini-2.5-flash-lite`：已不對新用戶開放
- `gemini-2.0-flash`：額度用盡
- **`gemini-3.5-flash-lite`：可用（本次採用）**

## 待處理
- 御神書 OCR 已完成（含 10 頁缺頁逐頁補齊）
- KB 內容 push GitHub（用戶說找時間再做）

## 系統狀態
- GUI (PID 4536) 運行中（08:19 重啟，已載入新 code）
- 任務 #1064 已完成（69/111 檔）
- Gemini key 存於 `C:\Users\hpspy\.gemini_api_key`；`google-genai` SDK 已安裝
- Groq 額度：音訊轉錄走 ASD（音訊秒數/日），與 OCR TPD 分開計算

## 備份位置
- 系統備份：`D:\AI-Agent-Workspace\backup_20260802_*\`
