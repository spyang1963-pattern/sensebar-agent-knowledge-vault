# YouTube 知識庫建立器 - 使用指南

## 快速開始（5 分鐘建立新專家的知識庫）

---

### 第 1 步：建立新專案資料夾

在您的電腦上建立一個新資料夾，例如：
```
D:\!!AI agent 專案夾\志祺七步的 AI 知識庫
```

---

### 第 2 步：複製腳本到新資料夾

將以下 3 個檔案從「三師爸的 AI agent 學習 AI agent」資料夾複製到新資料夾：

| 檔案 | 來源位置 |
|------|----------|
| `config.yaml` | 設定檔 |
| `extract_videos.py` | 篩選影片腳本 |
| `download_all_subs.py` | 下載字幕腳本 |

---

### 第 3 步：修改 config.yaml

用文字編輯器（記事本、VS Code 等）打開 `config.yaml`，修改以下內容：

```yaml
# ============================================
# YouTube 知識庫建立器 - 設定檔
# ============================================

# YouTube 頻道設定
channel:
  # 改成新專家的頻道網址
  url: "https://www.youtube.com/@志祺七步"
  
  # 改成新專家的主題關鍵字
  keywords:
    - "ai"
    - "科技"
    - "商業"
    - "人工智慧"

# 輸出設定（通常不用改）
output:
  subtitle_langs:
    - "zh-Hant"
    - "zh-TW"
    - "zh"
    - "en"
  
  folders:
    clipping: "Clipping"
    creation: "創作庫"
    knowledge: "知識庫"
```

**注意事項：**
- `url`：YouTube 頻道網址，格式為 `https://www.youtube.com/@頻道名稱`
- `keywords`：要篩選的關鍵字，小寫即可（腳本會自動比對不分大小寫）
- `subtitle_langs`：字幕語言優先順序，通常不用改

---

### 第 4 步：開啟命令列視窗

1. 按 `Win + R`
2. 輸入 `powershell` 按 Enter
3. 切換到新資料夾：
```powershell
cd "D:\!!AI agent 專案夾\志祺七步的 AI 知識庫"
```

---

### 第 5 步：安裝必要工具（僅第一次）

```powershell
pip install yt-dlp pyyaml
```

---

### 第 6 步：執行篩選影片

```powershell
python extract_videos.py
```

**預期輸出：**
```
頻道: https://www.youtube.com/@志祺七步
關鍵字: ai, 科技, 商業, 人工智慧

videos 分頁: XXX 支
streams 分頁: XXX 支
去重後共 XXX 支影片

匹配 XX 支影片

儲存過濾清單至: 志祺七步_ai_videos.md
儲存全部清單至: 志祺七步_all_videos.md
儲存 URL 清單（XX 筆）至: 志祺七步_ai_urls.txt
```

---

### 第 7 步：執行下載字幕

```powershell
python download_all_subs.py
```

**預期輸出：**
```
頻道: 志祺七步
字幕語言: zh-Hant, zh-TW, zh, en
Total videos to process: XX

[1/XX] Fetching metadata for https://www.youtube.com/watch?v=...
  Downloading subtitles for: 影片標題...
  [Success] Saved to: 影片標題.md
...
All tasks completed!
```

**時間預估：** 每支影片約 3-5 秒，100 支影片約 5-8 分鐘

---

### 第 8 步：檢查成果

新資料夾中會出現：
```
志祺七步的 AI 知識庫/
├── config.yaml                    # 設定檔
├── extract_videos.py              # 篩選腳本
├── download_all_subs.py           # 下載腳本
├── 志祺七步_ai_urls.txt           # AI 相關影片 URL
├── 志祺七步_ai_videos.md          # AI 影片清單表格
├── 志祺七步_all_videos.md         # 頻道全部影片清單
├── Clipping/                      # 字幕逐字稿（56+ 個 .md 檔）
├── 創作庫/                        # 您的教材（空）
└── 知識庫/                        # Agent 整理的知識（空）
```

---

### 第 9 步：用 Obsidian 開啟（可選）

1. 打開 Obsidian
2. 點「開啟資料夾為儲存庫」
3. 選擇新資料夾
4. 就能看到所有字幕檔案

---

### 第 10 步：建立 GitHub 備份（可選）

```powershell
git init
git add .
git commit -m "init: 建立志祺七步知識庫"
gh repo create 志祺七步-knowledge-vault --public --source=. --remote=origin --push
```

---

## 常見問題

### Q: 如何更新（三師爸有新影片時）？
```powershell
cd "D:\!!AI agent 專案夾\三師爸的 AI agent 學習 AI agent"
python extract_videos.py
python download_all_subs.py
```

### Q: 如何同時管理多個專家的知識庫？
每個專家建立一個獨立的資料夾，各自有各自的 `config.yaml`。

### Q: 關鍵字怎麼選？
- 先到該專家的 YouTube 頻道瀏覽影片標題
- 找出常出現的主題詞
- 建議 5-10 個關鍵字

### Q: 下載失敗怎麼辦？
腳本會自動跳過失敗的影片，繼續處理下一支。失敗的影片通常是没有字幕的直播或短影片。

---

## 完整指令清單（快速參考）

```powershell
# 1. 切換到專案資料夾
cd "D:\!!AI agent 專案夾\新專家的知識庫"

# 2. 安裝工具（僅第一次）
pip install yt-dlp pyyaml

# 3. 篩選影片
python extract_videos.py

# 4. 下載字幕
python download_all_subs.py

# 5. 建立 GitHub 備份（僅第一次）
git init
git add .
git commit -m "init"
gh repo create 新倉庫名稱 --public --source=. --remote=origin --push

# 6. 之後更新
git add .
git commit -m "update"
git push
```
